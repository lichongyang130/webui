// Views/copies counters and admin edits must show immediately.
export const dynamic = "force-dynamic";

export default function ItemLayout({ children }: { children: React.ReactNode }) {
  return children;
}
