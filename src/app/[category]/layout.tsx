// Category pages reflect admin changes (counts, new items) immediately.
export const dynamic = "force-dynamic";

export default function CategoryLayout({ children }: { children: React.ReactNode }) {
  return children;
}
